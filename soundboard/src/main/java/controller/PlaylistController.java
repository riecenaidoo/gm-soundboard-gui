package controller;

import model.Playlist;

public class PlaylistController {
    public static void play(Playlist playlist) {
        throw new UnsupportedOperationException(String.format("TODO: Play this playlist: %s", playlist));
//        API::play(playlist.getSongs());
    }
}
